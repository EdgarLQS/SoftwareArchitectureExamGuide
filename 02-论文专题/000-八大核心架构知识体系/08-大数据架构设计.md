# 大数据架构

## Lambda 架构

```
数据源 → Kafka → {批处理层（Spark）+ 速度层（Storm/Flink）} → 服务层 → 查询
```

## Kappa 架构

```
数据源 → Kafka → 流处理（Flink） → 服务层 → 查询
```

## 架构选型建议

- **Lambda**：对数据准确性要求高、有历史数据分析需求
- **Kappa**：实时性要求高、简化运维
