# 云原生架构核心知识点

> 整理自 Google Gemini 学习对话 | 软考架构师论文专题

---

## 一、云原生四大支柱

### 1. 微服务（Microservices）

| 对比维度 | 单体架构（Monolithic） | 微服务架构（Microservices） |
|---------|----------------------|--------------------------|
| 结构 | 所有功能打包在一起 | 拆分成独立的小服务 |
| 伸缩 | 必须复制整个应用 | 可独立伸缩特定服务 |
| 部署 | 任何改动需重新部署整体 | 可独立部署单个服务 |
| 故障隔离 | 单点故障影响全局 | 服务间相互隔离 |

**核心优势：独立伸缩（Independent Scaling）**
- 单体架构：如果"搜索"功能访问量巨大，必须复制整个庞大的应用来应对压力，非常浪费资源
- 微服务：可以只给"搜索"服务增加更多实例，而让其他访问量小的服务（如"修改资料"）保持现状

**实战场景：支付系统紧急修复**
- 单体架构：需要停止整个系统，修复后重新部署全部功能
- 微服务架构：只需修复并重新部署支付服务，其他服务（浏览、搜索）不受影响

---

### 2. 容器化（Containerization）

**核心概念：**
- 如果把微服务比作"小盒子"，容器化就是把这些小盒子打包成标准"集装箱"
- 容器提供一致的运行环境，消除"在我机器上能跑"的问题

**容器化的价值：**
- 环境一致性：开发、测试、生产环境完全一致
- 快速启动：秒级完成新实例启动
- 资源隔离：每个容器拥有独立的资源视图
- 便携性：可在任何支持容器的平台上运行

---

### 3. DevOps（开发运维一体化）

**思考题：如果你有 1000 个容器需要手动一个一个更新，最让你头疼的问题是什么？**

**答案：效率和一致性**
- 效率：手动登录 1000 台服务器执行更新，耗时极长
- 一致性：只要其中几台因网络抖动或配置微差导致更新失败，整个系统就会陷入版本不一致的混乱

**DevOps 解决方案：**
- 自动化运维流程
- 基础设施即代码（IaC）
- 持续监控与反馈

---

### 4. 持续交付（Continuous Delivery, CD）

**核心：建立自动化流水线**

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  提交       │ -> │  自动测试   │ -> │  自动构建   │ -> │  自动部署   │
│  (Commit)   │    │ (Testing)   │    │   (Build)   │    │(Deployment) │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

**流水线各阶段：**

| 阶段 | 说明 |
|------|------|
| 提交（Commit） | 开发者上传代码改动 |
| 自动测试（Automated Testing） | 系统自动运行成千上万个测试，确保新代码没有破坏原有功能 |
| 自动构建（Automated Build） | 测试通过后，系统自动把代码打包成一个新的容器镜像 |
| 自动部署（Automated Deployment） | 系统按照预设策略（如：一次更新 10%，观察没问题再继续），自动把新镜像推送到容器中 |

**Fail-Fast（快速失败）机制的好处：**

| 好处 | 说明 |
|------|------|
| 防止污染 | 坏代码不会进入生产环境，避免影响真实用户 |
| 快速反馈 | 开发者能立刻收到报错通知，在记忆最清晰时修复问题 |
| 降低成本 | 在流水线早期修复问题的代价，远小于在系统崩溃后进行抢修 |

---

## 二、Kubernetes（K8s）—— 云原生的"大脑"

### 核心定位

> 如果容器是集装箱，那么 Kubernetes 就是港口上的全自动调度系统

**K8s 职责：** 在拥有成千上万个容器的复杂系统中，确保每个容器都能在正确的时间、正确的地点运行

---

### 三大核心能力

#### 1. 自我修复（Self-healing）

**工作机制：**
- 如果一个容器"掉线"或崩溃了，K8s 会立刻发现并自动重启一个新的来替代它

**思考题：**
> 如果你告诉 K8s："我要 3 个支付服务的实例一直运行"，此时突然有一台服务器坏了，导致其中 1 个容器消失了，K8s 会发现"实际状态"（2 个）不等于"期望状态"（3 个），它会怎么做？

**答案：** K8s 会立刻在集群中寻找有空余资源的服务器，并自动启动一个新的容器，以恢复到你要求的 3 个状态

---

#### 2. 自动扩缩容（Auto-scaling）

**HPA（Horizontal Pod Autoscaler）工作原理：**

```
设定目标：当 CPU 使用率超过 60% 时，自动增加实例
    ↓
K8s 监控到指标超过阈值
    ↓
自动启动成百上千个新的容器实例（Pods）来分担压力
    ↓
当活动结束后流量下降，自动删除多余的容器
```

**实战价值：**
- 流量暴增时：自动扩容，无需人工干预
- 流量下降时：自动缩容，节省成本

---

#### 3. 服务发现与负载均衡（Service Discovery & Load Balancing）

**功能：**
- 自动给容器分配 IP
- 在多个容器之间平均分配流量
- 防止某个容器被"累死"

---

### 核心概念：声明式配置（Declarative Configuration）

**理解 K8s 的关键：**

> 你不需要命令它"去启动一个容器"，而是告诉它一个**"期望状态"（Desired State）**

**示例：**
```yaml
# 你告诉 K8s：
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
spec:
  replicas: 3  # "我要 3 个实例一直运行"
```

**K8s 的工作方式：**
- 你定义"终点"（我要 3 个）
- K8s 负责"路径"（怎么维持 3 个）
- K8s 持续监控实际状态，确保与期望状态一致

---

## 三、12-Factor App（十二要素应用）

### 核心原则

#### 1. 配置（Config）

**原则：** 不要把数据库密码硬编码在代码里，要通过"环境变量"注入

**好处：**
- 一份代码可以不经修改就在开发、测试、生产环境运行
- 安全性提升：敏感信息不进入代码仓库

**示例：**
```bash
# 传统方式（不推荐）
DATABASE_URL = "mysql://admin:password123@localhost:3306/mydb"

# 12-Factor 方式（推荐）
DATABASE_URL = os.getenv("DATABASE_URL")
# 通过环境变量注入：export DATABASE_URL="mysql://..."
```

---

#### 2. 无状态（Statelessness）

**原则：** 应用进程不应该在本地存储数据

**比喻：** 就像共享单车一样，你骑完即走，不留痕迹。数据应该存在专门的数据库里

**思考题：**
> 如果你的应用是"有状态的"（比如它把用户上传的文件存在了自己那个小容器的硬盘里），当 K8s 因为故障把这个容器关掉并在另一台机器重启时，用户上传的文件会发生什么？

**答案：** 数据会丢失！因为容器是"临时性"的（Ephemeral），它们随时可能因为故障、自动缩容或版本更新而被替换

**解决方案：**

| 方案 | 说明 |
|------|------|
| 分离（Separation） | 把"计算"（运行代码的容器）和"存储"（数据库或云存储）分开 |
| 持久化（Persistence） | 使用 K8s Persistent Volumes (PV)，像给容器插了个"外接硬盘"，即使容器坏了换成新的，数据也还在 |

---

#### 3. 其他关键要素

| 要素 | 说明 |
|------|------|
| 代码库（Codebase） | 一份代码库对应多个部署（开发、测试、生产） |
| 依赖（Dependencies） | 显式声明所有依赖，不隐式依赖系统工具 |
| 后端服务（Backing Services） | 数据库、缓存等作为附加资源，不绑定到应用 |
| 构建、发布、运行（Build, Release, Run） | 严格分离构建和运行阶段 |
| 进程（Processes） | 应用以一个或多个无状态进程运行 |
| 端口绑定（Port Binding） | 通过端口绑定提供服务，不依赖外部服务器 |
| 并发（Concurrency） | 通过进程模型处理并发，而非操作系统线程 |
| 易处置（Disposability） | 快速启动和优雅关闭，实现弹性扩缩容 |
| 开发/生产环境一致 | 保持开发、预发布、生产环境尽可能相同 |
| 日志（Logs） | 将日志视为事件流，不管理日志文件 |
| 管理进程（Admin Processes） | 管理任务作为一次性进程运行 |

---

## 四、实战场景：电商大促流量暴增 50 倍

### 场景设定

> 你是一家大型电商平台的首席架构师。半小时后，一年一度的"大促活动"就要开始了。根据预测，届时你的系统访问量将瞬间激增**50 倍**。

### 应对方案

#### 1. 微服务层面：隔离与保护

**问题：** 如果流量太猛导致"支付系统"反应变慢，由于它是独立的，我们可以采取什么手段来保护它，而不至于让整个购物网站（如浏览商品、搜索）都瘫痪？

**解决方案：**

| 策略 | 说明 |
|------|------|
| 独立扩容 | 给"浏览和搜索"服务增加容器实例，确保用户还能正常看商品 |
| 熔断（Circuit Breaking） | 如果支付服务压力过载，系统会自动拒绝部分请求并返回"排队中"，而不是让整个支付服务彻底崩溃（像电路里的保险丝） |
| 限流（Rate Limiting） | 设置每秒请求数上限，超出部分返回友好提示 |

---

#### 2. Kubernetes 层面：自动扩缩容

**问题：** 面对这 50 倍的流量，K8s 的哪项功能可以帮你自动增加服务器上的应用实例？

**答案：HPA（Horizontal Pod Autoscaler）**

**配置示例：**
```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: payment-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: payment-service
  minReplicas: 3
  maxReplicas: 100
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 60  # CPU 使用率超过 60% 时扩容
```

**执行流程：**
1. Prometheus 监控到请求延迟（Latency）上升
2. HPA 检测到指标超过阈值，向 Deployment 发出扩容指令
3. K8s 迅速调度新 Pod，并通过 Service 自动实现流量的负载均衡

---

#### 3. CI/CD 层面：紧急修复

**问题：** 如果在活动期间，你必须修复一个紧急漏洞（Bug），CI/CD 流水线如何能让你在几分钟内把修复补丁发布给全球用户？

**解决方案：**

```
代码修复 -> CI（持续集成）跑测试 -> CD（持续部署）生成新镜像 -> 推送到 K8s
```

**优势：**
- 自动化流程让你不需要手动去 1000 个容器里改代码
- K8s 执行滚动更新（Rolling Update），逐个替换旧容器
- 确保在修复 bug 的过程中，服务依然是在线可用的

---

### 遇到的难题与解决方案

#### 问题：数据库连接池爆满

**场景：** 虽然 K8s 成功扩容了 100 个容器，但数据库却因为连接数过多崩溃了

**解决方案：增加缓存层（Redis）**

**缓存策略选择：**

| 策略 | 说明 |
|------|------|
| 旁路缓存（Cache Aside） | 先读缓存，未命中再读库并回写 |
| 缓存预热 | 活动前将热点数据（库存、商品信息）提前加载到 Redis |
| 防雪崩 | 通过 TTL（生存时间）或随机过期时间防止缓存雪崩 |

**分布式锁处理并发竞争：**
> 在 50 倍流量下，如果多个请求同时修改同一件商品的库存，可能会导致数据超卖。我们利用 Redis 的 SETNX 实现了分布式锁，确保在高并发下数据的一致性。

**性能提升量化：**
- 数据库 IOPS 降低了 70%
- 系统响应时间（RT）从 500ms 降低到 50ms

---

## 五、云原生数据库核心特性

### 存储计算分离架构

| 特性 | 架构师视角 | 实战描述 |
|------|-----------|---------|
| 存储计算分离 | 计算节点可根据流量秒级扩容，存储按需自动增长 | "在 50 倍流量冲击下，我们快速增加了只读节点，配合 Redis 缓存分担读压力" |
| Serverless 弹性 | 自动应对峰谷流量，无需手动预估配置 | "利用 Redis 缓存高频热点数据，减少云数据库 Serverless 频繁扩容的延迟" |
| 高可用与自愈 | 多副本强一致性，节点故障秒级切换 | "当云数据库发生主备切换时，Redis 作为缓冲层确保了前端业务的短暂可用" |

### 选型依据

| 业务需求 | 技术选型 |
|---------|---------|
| 订单数据量已达数 TB，传统扩容方式停机时间过长 | 选择云原生数据库（如阿里云 PolarDB 或华为云 GaussDB） |
| 需要分钟级增加只读节点 | 看重其共享存储架构和秒级扩容能力 |

### 落地难点与应对

#### 难点 1：缓存一致性

**问题：** Redis 缓存与云原生数据库在极端高并发下的数据不一致

**对策：**
- 采用 Canal 监听数据库 Binlog 异步更新缓存
- 配合 Redis 分布式锁处理库存扣减

#### 难点 2：SQL 慢查询性能波动

**问题：** 多节点并发下，复杂 SQL 可能拖累共享存储

**对策：**
- 利用云原生数据库的并行查询（Parallel Query）技术
- 将长事务进行拆分

#### 难点 3：数据库连接数爆炸

**对策：**
- 使用数据库代理（Database Proxy）进行连接池管理
- 实施连接复用，减少直接连接数据库的开销

---

## 六、软考架构师论文写作指南

### 论文结构建议

```
1. 摘要（Abstract）- 约 300 字
2. 引言与背景（Introduction）
3. 核心技术选择与应用（Main Body）
4. 遇到的问题与总结（Challenges & Reflection）
```

---

### 摘要写作公式

> **背景 → 困境 → 技术方案 → 结果**

**示例：**

> 本文以某大型零售企业的全渠道订单管理系统（OMS）为背景，我担任系统架构师，负责技术选型、数据库架构设计及高可用方案制定。该系统日均订单量达百万级，但在年度大促期间，瞬时访问量激增 50 倍，传统单体架构 CPU 负载达到 90% 以上，数据库连接池频繁爆满，存在系统瘫痪风险。
>
> 为解决核心支付链路的拥堵，本文采用云原生架构方案：基于微服务拆分实现服务解耦与独立伸缩；利用 Kubernetes HPA 实现自动扩缩容；引入 Redis 缓存层减轻数据库压力；通过 CI/CD 流水线实现分钟级紧急修复。
>
> 实施效果：大促期间 TPS 提升 300%，平均响应时间降至 50ms，系统平稳度过流量高峰。

---

### 正文核心论点

#### 1. 解耦与隔离

描述如何拆分微服务，确保搜索、浏览与支付互不影响

#### 2. 弹性伸缩设计

详细写 K8s 的 HPA 配置，根据 CPU/内存指标自动横向扩展

#### 3. 自动化流水线

描述 CI/CD 如何保证在流量高峰期也能安全、快速地推送紧急修复

---

### 论文题目建议

| 题目 | 特点 |
|------|------|
| 《论云原生数据库在企业信息系统架构中的应用与优化》 | 标准且稳健，适合系统性地论述从选型到实施的全过程 |
| 《基于云原生数据库的大规模高并发系统架构设计》 | 针对性强，非常适合结合"流量暴增"和"Redis 缓存"场景进行深入挖掘 |
| 《论企业信息系统从传统数据库向云原生数据库的迁移与演进》 | 侧重于"架构演进"，适合描写新旧系统对比及迁移过程中的技术挑战 |

---

### 论文核心公式

> **场景 + 问题 + 云原生技术方案 + 实施细节 + 最终效果**

---

## 七、关键术语对照表

| 英文术语 | 中文 | 说明 |
|---------|------|------|
| Microservices | 微服务 | 将大型应用拆分为小型独立服务 |
| Containerization | 容器化 | 将应用打包成标准化的运行单元 |
| Kubernetes (K8s) | 容器编排系统 | 自动化容器部署、扩缩容和管理 |
| HPA | 水平 Pod 自动伸缩 | 根据指标自动调整 Pod 副本数 |
| CI/CD | 持续集成/持续部署 | 自动化软件交付流水线 |
| Declarative Configuration | 声明式配置 | 定义期望状态，系统自动达成 |
| Stateless | 无状态 | 应用不存储本地数据，数据外置 |
| Circuit Breaking | 熔断 | 服务过载时自动拒绝请求保护系统 |
| Cache Aside | 旁路缓存 | 先读缓存，未命中再读库并回写 |
| Distributed Lock | 分布式锁 | 多实例环境下保证数据一致性的锁机制 |

---

## 八、学习总结

### 云原生完整拼图

```
┌─────────────────────────────────────────────────────────────┐
│                    云原生架构体系                            │
├─────────────┬─────────────┬─────────────┬─────────────────────┤
│   微服务     │    容器     │  DevOps    │   持续交付          │
│  解耦功能    │  标准化打包  │  自动化运维  │   快速修复和上线    │
│  独立面对压力 │  随处运行   │            │                     │
├─────────────┴─────────────┴─────────────┴─────────────────────┤
│                    Kubernetes（大脑）                          │
│          自动修补、扩缩容、管理容器                            │
├───────────────────────────────────────────────────────────────┤
│                    设计原则（12-Factor App）                    │
│          确保应用是"无状态"的，像云一样灵活                     │
└───────────────────────────────────────────────────────────────┘
```

### 核心思想

| 思想 | 说明 |
|------|------|
| 解耦 | 功能解耦、存储计算解耦、部署解耦 |
| 自动化 | 测试自动化、部署自动化、运维自动化 |
| 弹性 | 按需伸缩、故障自愈、资源优化 |
| 声明式 | 定义期望状态，系统自动达成和维持 |
| 可观测性 | 全链路监控、日志、追踪 |

---

## 九、高并发架构设计模式

### 1. 缓存策略模式

#### 旁路缓存（Cache Aside）

```
┌─────────────────────────────────────────────────────────────┐
│                    Cache Aside 模式流程                      │
│                                                             │
│   读请求：                                                  │
│   应用 → 查询缓存 → 命中？→ 是 → 返回数据                   │
│                    ↓ 否                                     │
│         查询数据库 → 写入缓存 → 返回数据                    │
│                                                             │
│   写请求：                                                  │
│   应用 → 更新数据库 → 删除缓存 (删除而非更新)                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**关键点：**
- 写操作时**删除缓存**而非更新缓存，避免并发写导致的不一致
- 缓存设置合理的 TTL，防止数据永久过期

#### 缓存预热（Cache Warm-up）

**适用场景：** 大促活动前、新系统上线

**实施步骤：**
1. 活动前 1 小时，通过离线任务将热点数据加载至 Redis
2. 按照热度分级：Top 100/Top 1000/Top 10000
3. 设置不同的过期时间：热点数据永不过期 + 异步刷新

#### 缓存雪崩防护

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 缓存雪崩 | 大量缓存同时过期 | 设置随机 TTL，避免集体过期 |
| 缓存击穿 | 热点 Key 过期 + 高并发 | 分布式锁 + 单线程重建缓存 |
| 缓存穿透 | 查询不存在的数据 | 布隆过滤器 + 空值缓存 |

---

## 十、分布式事务与幂等性设计

### 1. 幂等性设计（Idempotency）

**定义：** 同一请求多次执行，系统最终的处理结果都是一致的

**RequestID 生成策略：**

```
改进雪花算法（Snowflake）：
┌──────────────┬──────────────┬──────────────┐
│  时间戳 41 位  │  机器 ID 10 位  │  序列号 12 位  │
│ (69 年范围)    │ (1024 个实例)  │ (4096/毫秒)   │
└──────────────┴──────────────┴──────────────┘
```

**幂等性校验流程：**
1. 前端或网关层生成 RequestID 并附带在请求头中
2. 服务端使用 `SETNX RequestID processing 30` 尝试占位
3. 如果返回 1，说明是首次请求，继续处理业务
4. 如果返回 0，说明是重复请求，查询 Redis 中的处理状态并直接返回
5. 业务处理完成后，更新 Redis 中的状态为 success/fail

### 2. 分布式锁实现

**Redis SETNX 实现：**

```java
// 获取分布式锁
boolean locked = redis.setnx(lockKey, requestId, 30);  // 30 秒过期

if (!locked) {
    // 锁已被占用，可能是重复请求或正在处理中
    return getProcessingStatus(orderId);
}

try {
    // 执行业务逻辑
    return executeBusinessLogic();
} finally {
    // 释放锁（Lua 脚本保证原子性）
    releaseLock(lockKey, requestId);
}
```

**关键点：**
- 使用唯一 requestId 标识锁的持有者
- 释放锁时校验 requestId，防止误删他人锁
- 使用 Lua 脚本保证原子性

---

## 十一、服务稳定性保障机制

### 1. 熔断（Circuit Breaking）

**工作原理：**
```
┌─────────────────────────────────────────────────────────────┐
│                    熔断器状态转换                            │
│                                                             │
│   关闭 (Closed) → 失败次数超过阈值 → 打开 (Open)            │
│       ↑                                    │                │
│       │                                    ↓                │
│   半开 (Half-Open) ← 冷却时间结束 ← [拒绝所有请求]           │
│       │                                                    │
│       ↓ 试探请求成功                                         │
│   关闭 (Closed) - 恢复正常                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**配置参数：**
- 失败阈值：连续失败 5 次触发熔断
- 冷却时间：30 秒后进入半开状态
- 试探请求：半开状态下允许 1 个请求通过

### 2. 限流（Rate Limiting）

**常见算法：**

| 算法 | 原理 | 适用场景 |
|------|------|---------|
| 固定窗口计数器 | 单位时间内计数 | 简单场景，存在临界突变 |
| 滑动窗口计数器 | 细分时间片滑动 | 更平滑的限流 |
| 令牌桶算法 | 固定速率产生令牌 | 允许突发流量 |
| 漏桶算法 | 固定速率流出 | 严格限制速率 |

**配置示例（令牌桶）：**
```yaml
rate_limiting:
  algorithm: token_bucket
  bucket_capacity: 1000    # 桶容量（允许突发的请求数）
  refill_rate: 100/s       # 令牌补充速率
```

### 3. 降级（Fallback）

**降级策略：**

| 降级级别 | 触发条件 | 降级动作 |
|---------|---------|---------|
| 服务降级 | 服务响应超时 | 返回缓存数据或默认值 |
| 功能降级 | 系统负载过高 | 关闭非核心功能（如推荐、评论） |
| 读服务降级 | 数据库压力过大 | 只读模式，暂停写操作 |

---

## 十二、云原生架构论文写作素材

### 1. 项目背景模板

```
本文以 [项目名称] 为背景，我担任 [角色]，负责 [职责范围]。
该系统 [系统规模描述]，但在 [特定场景] 期间，瞬时访问量激增 [X] 倍，
传统架构出现 [具体问题 1]、[具体问题 2]，存在系统瘫痪风险。
```

### 2. 技术方案模板

```
为解决 [核心问题]，本文采用 [云原生技术方案]：
1. 基于 [技术 1] 实现 [目标 1]
2. 利用 [技术 2] 实现 [目标 2]
3. 引入 [技术 3] 减轻 [压力源]
4. 通过 [技术 4] 实现 [目标 4]
```

### 3. 效果量化模板

```
实施效果：[量化指标 1]，[量化指标 2]，系统平稳度过流量高峰。

参考指标：
- TPS 提升 300%（从 5 万到 30 万）
- 平均响应时间降至 50ms（降低 90%）
- 数据库 IOPS 降低 70%
- 资源成本节省 75%
- 系统可用性达 99.99%
```

### 4. 反思与展望模板

```
本次架构设计虽成功支撑了 [X] 倍的瞬时洪峰，但在反思中也意识到，
随着链路复杂度的增加，[问题 1：如可观测性] 成了保障系统稳定的关键。
我们计划引入 [解决方案 1]，通过 [具体实现方式]，实现 [预期效果]。

同时，考虑到 [问题 2：如单点故障] 可能引发的 [风险描述]，
我们正在设计 [解决方案 2]，确保在极端情况下核心业务的持续可用。
```

---

## 十三、软考高频考点总结

### 云原生架构考点

| 考点 | 考查形式 | 备考要点 |
|------|---------|---------|
| 微服务架构 | 论文/案例 | 服务拆分原则、独立部署、故障隔离 |
| Kubernetes | 论文/案例 | HPA 自动扩缩容、自我修复、声明式配置 |
| 容器化 | 论文 | Docker 打包、环境一致性、快速启动 |
| 持续交付 | 论文 | CI/CD 流水线、滚动更新、快速修复 |
| 缓存设计 | 论文/案例 | Redis 缓存策略、分布式锁、缓存一致性 |
| 云原生数据库 | 论文 | 存储计算分离、Serverless、弹性扩容 |

### 论文写作 Checklist

**写作前：**
- [ ] 确定论文题目和方向
- [ ] 准备项目背景材料（项目名称、规模、个人角色）
- [ ] 准备量化指标数据
- [ ] 列出核心技术关键词

**写作中：**
- [ ] 摘要涵盖背景→困境→方案→结果
- [ ] 正文每个论点都有具体实施细节
- [ ] 使用专业术语（HPA、Circuit Breaking、Cache Aside 等）
- [ ] 量化效果数据有说服力
- [ ] 难点与应对策略真实可信

**写作后：**
- [ ] 检查字数是否符合要求（2000-2500 字）
- [ ] 检查逻辑是否连贯
- [ ] 检查专业术语使用是否准确
- [ ] 检查是否有错别字和语法错误

---

*整理时间：2026 年 4 月 | 适用考试：系统架构设计师（2026 年 5 月）*
